# 更新日志

## v1.0.1（2026-08-15）

### 🐛 修复

- **adb「uiautomator dump 退出码 139」误判为失败**
  MuMu 15 / Android 15 模拟器上，uiautomator 会在「文件写好后、进程退出关停线程时」段错误（SIGSEGV），把退出码变成 139——但 XML 产物是好的。旧代码按退出码判断成败，导致每次都报「uiautomator dump 失败」。现改为**按 dump 文件是否写出了有效 XML 判断**，不再依赖退出码。
- **题干 ID 前缀识别不全**
  只认 `2022U2-32` 这类「年份后 U 前缀」的题号，`2022ESU1-1` 等格式识别不了，导致题干被当成评论区。正则放宽为 `20xx[字母数字]-数字`。
- **A3/A4 共用题干（病例）丢失**
  A3/A4 型题的病例题干无法并入记录。现把子题前的病例正文并入题干（保留独立 `case_stem` 字段），AI 讲解与速通手册带上完整上下文。
- **`A3/A4型题` 题型识别为 None**
  题型正则支持斜杠形式（A1/A2/A3/A4/B1/C 型题等）。
- **adb 缺失时启动崩溃**
  找不到 adb 时程序不再崩溃，界面引导到 `config.toml` 配置路径。

### ✨ 新增

- **薄弱知识点升级**
  - 从每道题的一句话考点（ai_summary）自动生成，取消「仅显示 8 条」上限，随新题加入自动更新；
  - 每条知识点可点击，跳转到手册内对应题目卡片（自动展开）；
  - F9 即使没有待整理的新题，也会重新生成手册。
- **adb 自动探测增强**
  新增 MuMu Player 各版本（12/15 等）通用目录 `MuMuPlayer*/nx_device/*/shell/adb.exe` 的自动探测。
- **诊断工具**（`tools/`）
  - `诊断工具.bat`：一键抓取 adb 环境、复现 dump、导出崩溃日志，用于远程排障；
  - `抓取界面XML.bat`：抓取当前屏幕界面 XML + 截图，用于解析问题排查。

### 🔧 优化

- 一句话考点（ai_summary）生成要求更详细：30~60 字，写清关键鉴别点、易错点。
- 移除 AI 分组级薄弱点生成（省 token，且原分组薄弱点无法关联具体题目）。

### 📦 发布包

`release/医考帮备考助手-v1.0.1.zip`（exe + config.toml + taxonomy.json + .env.example，exe 与旧版直接替换即可，数据保留在 `data/`）。

---

## v1.0.0（2026-08-13）

首版：F7 两按式错题记录 / F8 一键 AI 讲解 / F9 速通手册整理。
