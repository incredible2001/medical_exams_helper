# 医考帮备考助手 (medical_exams_helper) — 设计文档

> 执业医师笔试辅助工具：刷题仍在医考帮完成，本工具负责「不会的题问 AI + 错题收集 + 生成考前速通手册」。
> 目标用户：Windows + MuMu 模拟器 + 医考帮，纯选择题备考，总分 360/600 及格。

---

## 1. 环境与依赖

| 项 | 值 |
|---|---|
| OS | Windows 11 |
| 模拟器 | MuMu Player 12（adb 端口 `127.0.0.1:16384`） |
| adb 路径 | 自动探测（环境变量、常见安装目录），可配置覆盖 |
| Python | 3.14（`D:\Python\python.exe`） |
| DeepSeek | base_url `https://api.deepseek.com`，模型 `deepseek-v4-flash`，**非多模态** |

## 2. 三大能力（对应三个热键 + 侧边窗按钮）

### ① F7「记录」—— 错题收集（两按式）
- **第一按**：题干区在屏幕上时按 F7 → 完整记录（题干、选项、正确答案/你的答案、题号、试卷标签、考点还原、题型、统计）
- **第二按**：滚到评论区、等评论加载完，再按 F7 → 抓取当前可见评论 + 逐选项解析 blob，**并入同一道题**
- 合并匹配（三条信号，按可靠性排序）：
  1. **时间邻近**：并入 3 分钟内的最近一条题干记录
  2. **试卷标签校验**：评论 dump 的试卷标签必须与记录一致
  3. **附加**：解析 blob 出现时校验正确答案字母（"（A对）" == "正确答案 A"）
- **兜底**：无匹配 → 评论存为「待绑定」，状态栏提示；会话内录到匹配题干时自动补绑
- 重复按 F7 同题自动合并（评论、解析累积）

### ② F8「解析」—— 一键 AI 讲解
- dump 当前屏 → 提取题干+选项 → 发 DeepSeek 流式生成逐选项分析 → 侧边窗显示
- 附带 App 自带「考点还原」作为权威参考
- **不自动记录**；侧边窗有「记录到错题本」按钮，点了连同 AI 解析一起存

### ③ F9「整理」—— 增量生成速通手册
- 分批（20~30 题/次）调用 DeepSeek，只处理未整理的新记录
- 每批同时做三件事：生成每题错题卡、归类到分组、更新该组薄弱知识点清单
- 产出：`速通手册/<分组>.md` + 合并版《考前速通手册.html》（目录/可折叠/打印样式）

## 3. 采集层（实测验证）

- **主通道**：`adb exec-out uiautomator dump` + `adb exec-out screencap -p`，读视图节点树，无需 OCR
  - 题干/选项：TextView 存全文，即使部分滚出屏幕也能取到完整文本
  - 答案行：`答案：正确答案 A，你的答案 D` → 自动判对错
  - 评论：RecyclerView 虚拟列表，**只有当前加载渲染的评论在 dump 中**，滚动后需再按 F7 补抓
  - **已验证**：滚入评论区后题干/选项/题号会被回收出树，必须第一按在题干屏完成
- **兜底**：RapidOCR 本地识别（仅 uiautomator 取不到文字时启用）

## 4. 图片题处理

- 执业医师含图题（心电图、影像、病理切片）。DeepSeek 看不了图
- 记录/解析时检测题干区域 ImageView 控件 → 从截图裁剪保存到数据目录 → 记录标注「本题含图，AI 解析不含图像内容」

## 5. 数据层（SQLite）

- 表：`questions`（题干规范化哈希 = 唯一 ID）、`comments`、`ai_explanations`、`record_state`（已整理标记）
- 去重：题干规范化（去标点/空白）后哈希；重复记录自动合并
- 系统归类：整理时由 DeepSeek 依据题干+考点还原自动判「临床系统」；章节刷题模式下读取屏幕上可见章节名作为标签

## 6. 手册分组（taxonomy.json，可配置）

| 分组 | 医考帮学科 |
|---|---|
| 基础医学 | 生理学、生物化学、病理学、病理生理学、药理学、微生物学、免疫学、解剖学 |
| 临床内科 | 内科学、诊断学、传染病学、神经病学、精神病学 |
| 临床外科 | 外科学 |
| 妇儿 | 妇产科学、儿科学 |
| 预防与公共卫生 | 预防医学、医学统计学 |
| 医学人文 | 卫生法、医学心理学、医学伦理学 |
| 中医学基础 | 中医学基础 |

## 7. 交互与 UI

- **侧边窗（tkinter，固定置顶旁侧）**：按钮区（记录/解析/整理/打开手册）+ 解析显示区（流式）+ 状态区（adb 状态、已记录总数、事件反馈）
- 热键 `F7/F8/F9`（config.yaml 可改），全局生效；窗口按钮与热键一一对应

## 8. 容错

- 未识别到题目 → 状态栏提示，不崩
- uiautomator dump 失败 → 自动重试 2 次
- DeepSeek 请求失败 → 题目照常记录，解析标记待重试，下次整理补
- 评论无题干 → 待绑定 + 提示「请回题干处按一次 F7」

## 9. 安全与开源（GitHub public，MIT）

- API Key 仅存环境变量 `.env`（`DEEPSEEK_API_KEY`），`.env` 已 gitignore；代码不打印/不记录 key
- 仓库只提交 `.env.example`（占位符）
- `data/`（SQLite、截图、图片裁剪、HTML 手册）整体 gitignore——含医考帮真题内容（受版权保护），不入仓库
- README 含隐私提示：「题目与评论会发送至 DeepSeek API 处理」

## 10. 技术栈

```
Python 3.14 + tkinter（UI，标准库）
keyboard（全局热键；如遇权限问题换 pynput）
openai SDK（DeepSeek 兼容，base_url=https://api.deepseek.com，model=deepseek-v4-flash）
sqlite3（存储，标准库）
subprocess（adb）
rapidocr_onnxruntime（OCR 兜底，可选安装）
python-dotenv（.env 读取）
```

## 11. 明确不做（v1）

- 不自动刷题/自动点击/自动翻页（半自动：人控制节奏，工具负责记录）
- 不生成变式自测题
- 不做评论自动追捕（v2 候选）
