# DeepSeek 调用（OpenAI 兼容协议）
openai>=1.40.0
# .env 密钥读取
python-dotenv>=1.0.0
# 全局热键（F7/F8/F9）；Windows 若提示权限不足，改用 pynput
keyboard>=0.13.5
# 图片题截图裁剪
pillow>=10.0.0

# OCR 兜底（可选，uiautomator 取不到文字时启用）：
# rapidocr_onnxruntime>=1.3.0
